group 'edge.woo'
version '1.0'

apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

def corelib = '/evn-dev/mservice/core'
def trd = '/evn-dev/mservice/trd'
def outputdir = '/evn-dev/mservice/core'
sourceSets {
    main {
        java {
            srcDirs = ["src/main/java"]
        }
        resources {
            srcDirs = ["src/main/resources"]
        }
    }
}
println(configurations.compile.fileCollection { trd })

dependencies {
    compile fileTree(dir: corelib, include: 'dataentity*.jar')
    compile fileTree(dir: corelib, include: 'orm*.jar')
    compile fileTree(dir: corelib, include: 'web*.jar')
    compile fileTree(dir: trd, include: 'protostuff*.jar')
    compile fileTree(dir: corelib, include: 'mservice-user*.jar')
    compile fileTree(dir: trd, include: 'javax.servlet-api*.jar')
    compile fileTree(dir: trd, include: 'spring-web-*.RELEASE.jar')
    compile fileTree(dir: trd, include: 'slf4j-api-*.jar')
    compile fileTree(dir: trd, include: 'jjwt*.jar')
    compile fileTree(dir: trd, include: 'fastjson-1.2.47.jar')
    compile fileTree(dir: trd, include: 'commons-codec*.jar')
}
task sourcesJar(type: Jar, dependsOn: build) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

task copytolib(type: Copy, dependsOn: build) {
    group 'build'
    description '拷贝本工程构建jar包到lib'
    from 'build/libs'
    into corelib
    exclude '*source*.jar'
}


task copyJarTodir(type: Copy) {
    group 'build'
    description '拷贝工程产生的build.jar到本地库'
    from 'build/libs'
    into corelib
    exclude '**/*.class'
}

tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}
test.ignoreFailures true
